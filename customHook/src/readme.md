// custom hooks  
// user data  example name, role and profile data
// navbar //profile // dashboard 

// useEffect // axios get 

// it is resubale function that contains react hooks (useState, useeffect) avoiding duplicating logic in different components .


// rules of custom hooks
1. the Function  name must start with use
2. It can use other react hooks.
3. it can calls them only  inside  the react   componets other custom hooks
4. Dont use or call them inside any loops or conditions

/// assignment what is terniary operator and HoC(high order  componenets ) with examples  and custom hooks examples by your own choice 


// Local storage 

local storage  stores data inside the browser 